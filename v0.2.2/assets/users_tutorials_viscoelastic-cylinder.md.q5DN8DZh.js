import{_ as o,o as t,c as a,an as i}from"./chunks/framework.BMe1Ravt.js";const s="/Kraken.jl/v0.2.2/assets/viscoelastic-cylinder-bc.K1dQ2mXB.svg",g=JSON.parse('{"title":"Viscoelastic Cylinder Tutorial","description":"","frontmatter":{},"headers":[],"relativePath":"users/tutorials/viscoelastic-cylinder.md","filePath":"users/tutorials/viscoelastic-cylinder.md","lastUpdated":null}'),r={name:"users/tutorials/viscoelastic-cylinder.md"};function d(n,e,c,l,h,p){return t(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="Viscoelastic-Cylinder-Tutorial" tabindex="-1">Viscoelastic Cylinder Tutorial <a class="header-anchor" href="#Viscoelastic-Cylinder-Tutorial" aria-label="Permalink to &quot;Viscoelastic Cylinder Tutorial {#Viscoelastic-Cylinder-Tutorial}&quot;">​</a></h1><p>This tutorial covers Kraken&#39;s <strong>viscoelastic</strong> validation case: Oldroyd-B flow past a confined cylinder, the standard benchmark for non-Newtonian LBM solvers. It reports the validated drag results honestly, including where the solver is proven and where it is not.</p><p>The log-conformation finite-volume viscoelastic solver (<code>_logfv</code>) <strong>runs on this branch</strong>: the driver <code>run_viscoelastic_logfv_cylinder_coupled_2d</code> is shipped and dispatched through the <code>viscoelastic</code> module, and a coarse smoke <code>.krk</code> is included so you can run the case end-to-end (see <a href="./@ref">Reproduce</a> below). The production <code>R = 50</code> tables were re-validated against rheoTool to <strong>≤ 1 % on <code>C_d</code></strong> (Wi ≤ 1).</p><div class="tip custom-block"><p class="custom-block-title">Coarse smoke vs the production sweep</p><p>The shipped <code>.krk</code> is a <em>quick smoke</em> (240×32, R = 8, 20 steps) confirming the solver dispatches and returns a <code>C_d</code>. It is <strong>not</strong> the converged <code>R = 50</code> table value; reproducing the validated drag requires the A100-class production sweep described under <a href="./@ref">Reproduce</a>.</p></div><h2 id="The-problem" tabindex="-1">The problem <a class="header-anchor" href="#The-problem" aria-label="Permalink to &quot;The problem {#The-problem}&quot;">​</a></h2><p>A cylinder held on the axis of a planar channel, with a viscoelastic <strong>Oldroyd-B</strong> fluid flowing past it. Oldroyd-B is the simplest viscoelastic model: a Newtonian solvent (viscosity <code>η_s</code>) plus a polymer contribution (viscosity <code>η_p</code>, relaxation time <code>λ</code>) whose elastic stress is carried by a conformation tensor. The total viscosity is constant — Oldroyd-B is <em>not</em> shear-thinning — so the rich behaviour comes purely from elasticity.</p><p>The dimensionless drivers are:</p><ul><li><p>the <strong>Weissenberg number</strong> <code>Wi = λ U / R</code> (elasticity strength), and</p></li><li><p>the <strong>viscosity ratio</strong> <code>β = η_s / (η_s + η_p)</code> (solvent fraction).</p></li></ul><p>The reported quantity is the <strong>drag coefficient</strong> <code>C_d</code> on the cylinder, compared to fine-mesh <strong>rheoTool</strong> (OpenFOAM-based, Oldroyd-B log-conformation) reference runs.</p><p><img src="'+s+'" alt=""></p><h2 id="Validated-results" tabindex="-1">Validated results <a class="header-anchor" href="#Validated-results" aria-label="Permalink to &quot;Validated results {#Validated-results}&quot;">​</a></h2><h3 id="Drag-at-Re-1,-the-production-resolution-R-50" tabindex="-1">Drag at <code>Re = 1</code>, the production resolution <code>R = 50</code> <a class="header-anchor" href="#Drag-at-Re-1,-the-production-resolution-R-50" aria-label="Permalink to &quot;Drag at `Re = 1`, the production resolution `R = 50` {#Drag-at-Re-1,-the-production-resolution-R-50}&quot;">​</a></h3><p>This is the benchmark gate. At cylinder resolution <code>R = 50</code> (the production mesh), Kraken&#39;s drag matches the rheoTool fine-mesh references to <strong>≤ 1 %</strong> across the validated Weissenberg range, with the default half-way bounce-back wall treatment, in Float64:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Wi</th><th style="text-align:right;">rheoTool fine-mesh <code>C_d</code></th><th style="text-align:right;">Kraken <code>C_d</code> vs reference</th></tr></thead><tbody><tr><td style="text-align:right;">0.1</td><td style="text-align:right;">130.43</td><td style="text-align:right;">≤ 1 %</td></tr><tr><td style="text-align:right;">0.5</td><td style="text-align:right;">119.71</td><td style="text-align:right;">≤ 1 %</td></tr><tr><td style="text-align:right;">1.0</td><td style="text-align:right;">120.40</td><td style="text-align:right;">≤ 1 %</td></tr></tbody></table><p>The drag-vs-<code>Wi</code> curve shows the expected non-monotone shape — a minimum near <code>Wi ≈ 0.5</code> followed by an upturn — reproducing the qualitative behaviour known for this benchmark.</p><h3 id="Viscosity-ratio-β-cross-validation" tabindex="-1">Viscosity-ratio (<code>β</code>) cross-validation <a class="header-anchor" href="#Viscosity-ratio-β-cross-validation" aria-label="Permalink to &quot;Viscosity-ratio (`β`) cross-validation {#Viscosity-ratio-β-cross-validation}&quot;">​</a></h3><p>Sweeping the solvent fraction <code>β</code>, Kraken and rheoTool agree to <strong>≤ 1 % at β = 0.3</strong> (and at <code>β = 0.59</code>). Both codes diverge from each other at <code>β ≤ 0.1</code>, which is therefore a <em>cross-code-validated</em> hard regime — a shared wall, not a Kraken-specific defect.</p><h3 id="Stability-is-not-the-same-as-convergence" tabindex="-1">Stability is not the same as convergence <a class="header-anchor" href="#Stability-is-not-the-same-as-convergence" aria-label="Permalink to &quot;Stability is not the same as convergence {#Stability-is-not-the-same-as-convergence}&quot;">​</a></h3><p>Kraken&#39;s half-way bounce-back cylinder is <strong>NaN-free up to <code>Wi = 10</code></strong> at both <code>R = 30</code> and <code>R = 50</code> — well past the <code>Wi ≈ 1</code> ceiling typical of the LBM-viscoelastic literature, and without the High-Weissenberg-Number-Problem blow-up that limits many solvers.</p><div class="warning custom-block"><p class="custom-block-title">High-Wi drag is not converged</p><p>Stability does not imply accuracy. <strong>Beyond <code>Wi ≈ 1–2</code> the high-Wi <code>C_d</code> is resolution-dependent and is not a validated datapoint.</strong> The relaxation time <code>λ = Wi · R / U</code> grows very large at high <code>Wi</code>, so a fixed 300 000-step run is far from steady, and the <code>R = 30</code> and <code>R = 50</code> resolutions disagree increasingly as <code>Wi</code> rises. The minimum (<code>Wi ≈ 0.5</code>) and the upturn (<code>Wi ≈ 1.5–3</code>) are qualitatively correct, but the absolute high-Wi drag would need a finer mesh and longer runs. <strong>The clean, validated result is <code>Wi ≤ 1</code> at <code>R = 50</code> (≤ 1 %).</strong></p></div><h3 id="First-normal-stress-difference-N1-—-an-open-gap" tabindex="-1">First normal-stress difference <code>N1</code> — an open gap <a class="header-anchor" href="#First-normal-stress-difference-N1-—-an-open-gap" aria-label="Permalink to &quot;First normal-stress difference `N1` — an open gap {#First-normal-stress-difference-N1-—-an-open-gap}&quot;">​</a></h3><p>Kraken logs the maximum first normal-stress difference <code>N1_max</code> along the wake (for example <code>N1_max = 8.4e-4</code> at <code>R = 50</code>, <code>Wi = 1</code>), and the <code>N1</code> trend is concordant with rheoTool (it grows with <code>Wi</code> and as <code>β</code> decreases in both codes). <strong>However, no rheoTool <code>N1</code> reference value was extracted</strong>, so a quantitative <code>N1</code> comparison is not available. The <code>N1</code> leg of the validation is therefore <strong>open</strong>: trends agree, but the absolute <code>N1</code> match is unverified and is not claimed as a pass.</p><h2 id="Reproduce" tabindex="-1">Reproduce <a class="header-anchor" href="#Reproduce" aria-label="Permalink to &quot;Reproduce {#Reproduce}&quot;">​</a></h2><p>The case is <code>benchmarks/krk/viscoelastic/cylinder_oldroyd_b.krk</code> (a copy also ships under <code>benchmarks/results/rheotool_compare/viscoelastic/</code>). It combines a cylinder <code>Obstacle</code> with an <code>oldroyd_b</code> <code>Rheology</code> block; the polymer parameters follow the diffusive scaling <code>ν_s = β·ν_total</code>, <code>ν_p = (1−β)·ν_total</code>, <code>λ = Wi · R / U</code>. See the <a href="./../krk-reference">KRK reference <code>Rheology</code> block</a> for the grammar:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Rheology oldroyd_b { nu_s = ...  lambda = Wi*R/u_mean }</span></span></code></pre></div><p>Run the shipped smoke directly on this branch:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Kraken</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">result </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> run_simulation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;benchmarks/results/rheotool_compare/viscoelastic/cylinder_oldroyd_b.krk&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@show</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Cd   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># coarse smoke (240×32, R = 8, 20 steps) — NOT the R = 50 table value</span></span></code></pre></div><p>This is a quick dispatch check (it returns a non-converged <code>C_d</code>). The validated <code>R = 50</code> tables (300 000 steps, CUDA Float64) were produced on <strong>Aqua (A100)</strong> via <code>bench/viscoelastic_logfv/run_ve_revalidate_r50_halfwaybb.pbs</code> and are <strong>not</strong> CI-reproducible — they require an A100-class run.</p><h2 id="Summary" tabindex="-1">Summary <a class="header-anchor" href="#Summary" aria-label="Permalink to &quot;Summary {#Summary}&quot;">​</a></h2><ul><li><p><strong>Quantitative gate: passed.</strong> <code>C_d</code> matches fine-mesh rheoTool to ≤ 1 % at <code>Wi ∈ {0.1, 0.5, 1.0}</code>, <code>R = 50</code>, <code>Re = 1</code>, half-way bounce-back, Float64.</p></li><li><p><strong><code>β</code> cross-validated</strong> to ≤ 1 % at <code>β = 0.3</code>; shared divergence at <code>β ≤ 0.1</code>.</p></li><li><p><strong>Stable (NaN-free) to <code>Wi = 10</code></strong>, but high-Wi <code>C_d</code> is resolution-divergent and unconverged — validated only for <code>Wi ≤ 1</code> (the qualitative min+upturn is reproduced).</p></li><li><p><strong><code>N1</code> gap:</strong> Kraken logs <code>N1_max</code> and the trend agrees with rheoTool, but no rheoTool <code>N1</code> reference was extracted, so the absolute <code>N1</code> match is unverified.</p></li><li><p><strong>Solver runs on this branch.</strong> The <code>_logfv</code> cut-link driver is shipped and dispatched via the <code>viscoelastic</code> module; the included <code>.krk</code> runs as a coarse smoke. The ≤ 1 % <code>C_d</code> tables are the A100 production sweep, not the local smoke.</p></li></ul><h2 id="Where-to-go-next" tabindex="-1">Where to go next <a class="header-anchor" href="#Where-to-go-next" aria-label="Permalink to &quot;Where to go next {#Where-to-go-next}&quot;">​</a></h2><ul><li><p>The <a href="./../krk-reference">KRK reference <code>Rheology</code> block</a> — the viscoelastic and non-Newtonian constitutive grammar.</p></li><li><p>The Newtonian <a href="./../../examples/04_cavity_2d">lid-driven cavity example</a> and <a href="./../benchmarks/cartesian-cavity">Cartesian cavity benchmark</a> — the validated baseline the viscoelastic path builds on.</p></li></ul>',32)])])}const m=o(r,[["render",d]]);export{g as __pageData,m as default};
