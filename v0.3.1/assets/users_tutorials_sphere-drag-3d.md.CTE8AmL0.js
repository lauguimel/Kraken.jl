import{_ as a,o as t,c as s,an as n}from"./chunks/framework.Bwq7wkjc.js";const r="/Kraken.jl/v0.3.1/assets/sphere-drag-3d-bc.RXwVjARa.svg",u=JSON.parse('{"title":"Sphere Drag 3D Tutorial","description":"","frontmatter":{},"headers":[],"relativePath":"users/tutorials/sphere-drag-3d.md","filePath":"users/tutorials/sphere-drag-3d.md","lastUpdated":null}'),o={name:"users/tutorials/sphere-drag-3d.md"};function i(l,e,d,c,h,p){return t(),s("div",null,[...e[0]||(e[0]=[n('<h1 id="Sphere-Drag-3D-Tutorial" tabindex="-1">Sphere Drag 3D Tutorial <a class="header-anchor" href="#Sphere-Drag-3D-Tutorial" aria-label="Permalink to &quot;Sphere Drag 3D Tutorial {#Sphere-Drag-3D-Tutorial}&quot;">​</a></h1><p>This tutorial is the hands-on companion to the <a href="./../benchmarks/sphere-drag-3d">3D STL sphere-drag benchmark page</a>. It walks through the <strong>3D STL-obstacle workflow</strong>: how to put an arbitrary triangulated surface into a Kraken simulation, set up an external flow around it, and measure the drag — using flow past a sphere at <code>Re = 20</code> as the worked example.</p><h2 id="The-problem" tabindex="-1">The problem <a class="header-anchor" href="#The-problem" aria-label="Permalink to &quot;The problem {#The-problem}&quot;">​</a></h2><p>A sphere held fixed in a uniform stream. We measure its <strong>drag coefficient</strong> <code>C_d = 2 F_x / (u_in² A)</code>, with <code>A</code> the projected frontal area (the <code>y</code>–<code>z</code> silhouette of the solid), and compare to the classic Clift, Grace &amp; Weber (1978) free-stream correlation <code>C_d = (24/Re)(1 + 0.15 Re^0.687)</code>, which gives <code>C_d ≈ 2.61</code> at <code>Re = 20</code>.</p><p>The key new ingredient versus the cavity tutorials is <strong>geometry from an STL file</strong>. Instead of an implicit inequality, the obstacle is a triangle mesh, voxelised onto the lattice, with an <strong>interpolated bounce-back</strong> (<code>wall=libb</code>) wall treatment so the curved surface and the cut-link momentum exchange (which gives the drag) are resolved sub-cell.</p><p><img src="'+r+`" alt=""></p><h2 id="The-.krk-file" tabindex="-1">The <code>.krk</code> file <a class="header-anchor" href="#The-.krk-file" aria-label="Permalink to &quot;The \`.krk\` file {#The-.krk-file}&quot;">​</a></h2><p>The 3D STL drag case (<code>examples/geometry_stl/sphere_stl_3d_drag.krk</code>) is:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># 3D sphere drag (raw LU, Re = 20)</span></span>
<span class="line"><span>Simulation mgeo7_sphere_drag D3Q19</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Domain L = 120 x 60 x 60  N = 120 x 60 x 60</span></span>
<span class="line"><span>Physics nu = 0.032</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Obstacle sph wall=libb stl(file = &quot;examples/geometry_stl/sphere_drag.stl&quot;)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Boundary west  velocity(ux = 0.04, uy = 0)</span></span>
<span class="line"><span>Boundary east  pressure(rho = 1.0)</span></span>
<span class="line"><span>Boundary south wall</span></span>
<span class="line"><span>Boundary north wall</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Run 10000 steps</span></span></code></pre></div><p>Block by block:</p><ul><li><p><strong><code>Simulation ... D3Q19</code></strong> — a 3D case on the D3Q19 stencil.</p></li><li><p><strong><code>Domain L = 120 x 60 x 60 N = 120 x 60 x 60</code></strong> — a <code>120×60×60</code> channel (here <code>L = N</code>, so coordinates are lattice units).</p></li><li><p><strong><code>Physics nu = 0.032</code></strong> — chosen so <code>ν = u_in · (2R) / Re</code>; with <code>u_in = 0.04</code>, sphere radius <code>R = 8</code>, this is <code>Re = 20</code>.</p></li><li><p><strong><code>Obstacle sph wall=libb stl(file = &quot;...sphere_drag.stl&quot;)</code></strong> — the STL geometry. The <code>wall=libb</code> selector turns on interpolated bounce-back, which is what makes the cut-link drag integrator on the curved surface accurate.</p></li><li><p><strong><code>Boundary west velocity(...)</code></strong> — uniform inflow; <strong><code>east pressure(rho = 1.0)</code></strong> — a constant-pressure outlet; the <code>north</code>/<code>south</code> walls bound the duct.</p></li></ul><p>The STL obstacle has optional placement parameters — <code>scale</code>, <code>translate=[x,y,z]</code>, <code>z_slice</code> — documented in the <a href="./../krk-reference">KRK reference</a>. For example, <code>sphere_stl_3d_lu.krk</code> uses <code>scale = 20.0</code> to enlarge a small bundled mesh.</p><h3 id="Physical-units-instead-of-raw-LU" tabindex="-1">Physical units instead of raw LU <a class="header-anchor" href="#Physical-units-instead-of-raw-LU" aria-label="Permalink to &quot;Physical units instead of raw LU {#Physical-units-instead-of-raw-LU}&quot;">​</a></h3><p>If you prefer to specify the geometry in millimetres and a Reynolds number rather than hand-tuning <code>nu</code> and the inflow speed, add a <code>Units { ... }</code> block and let the bridge compute them. The bundled <code>sphere_stl_3d_mm.krk</code> does exactly this:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Units { length = mm  L_ref = 0.2  R_LU = 4  Re = 1.0  scaling = acoustic }</span></span>
<span class="line"><span>Physics nu = auto</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Obstacle sph wall=libb stl(file = &quot;examples/geometry_stl/sphere.stl&quot;)</span></span>
<span class="line"><span>Boundary west velocity(ux = u_LU, uy = 0)</span></span></code></pre></div><p>The bridge resolves the <code>0.2 mm</code> reference length to <code>R_LU = 4</code> cells, solves for the LU viscosity (injected as <code>Physics nu</code>) and inflow speed (the variable <code>u_LU</code>), and applies the matching STL scale. See the <a href="./../krk-reference">KRK reference <code>Units</code> block</a>.</p><h2 id="Running-it" tabindex="-1">Running it <a class="header-anchor" href="#Running-it" aria-label="Permalink to &quot;Running it {#Running-it}&quot;">​</a></h2><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Kraken</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">run_simulation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;examples/geometry_stl/sphere_stl_3d_drag.krk&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>3D drag runs are GPU work. The benchmark uses CUDA Float64 on Aqua for the production sweep; a local CPU run of the <code>.krk</code> is a parse + voxelise smoke check rather than a converged drag measurement.</p><p>There is also a <strong>self-consistency twin</strong> that runs locally on a GPU and checks the STL drag path against the validated analytic-sphere driver:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=.</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test/test_sphere_stl_drag_krk.jl</span></span></code></pre></div><p>It reproduces the analytic-sphere driver <code>run_sphere_libb_3d</code> (itself validated against Clift) to <strong>0.4 %</strong> at matched lattice registration — the STL voxeliser and the analytic q-wall are placed on the <em>same</em> lattice obstacle for the comparison.</p><h2 id="Expected-result" tabindex="-1">Expected result <a class="header-anchor" href="#Expected-result" aria-label="Permalink to &quot;Expected result {#Expected-result}&quot;">​</a></h2><p>Confined drag is sensitive to the lateral blockage <code>D/W</code> (sphere diameter over duct width): the walls add drag, so <code>C_d</code> is higher than the free-stream value and falls as the walls recede. The benchmark page runs a blockage sweep at fixed resolution <code>R = 16</code> and extrapolates to the unbounded limit:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Blockage <code>D/W</code></th><th style="text-align:right;"><code>C_d</code></th></tr></thead><tbody><tr><td style="text-align:right;">20 %</td><td style="text-align:right;">5.438</td></tr><tr><td style="text-align:right;">14.3 %</td><td style="text-align:right;">4.470</td></tr><tr><td style="text-align:right;">10 %</td><td style="text-align:right;">3.830</td></tr><tr><td style="text-align:right;">8 %</td><td style="text-align:right;">3.582</td></tr><tr><td style="text-align:right;">6 %</td><td style="text-align:right;">3.381</td></tr><tr><td style="text-align:right;"><strong><code>D/W → 0</code> (quadratic LSQ)</strong></td><td style="text-align:right;"><strong>2.84</strong></td></tr><tr><td style="text-align:right;"><strong>Clift 1978 free-stream</strong></td><td style="text-align:right;"><strong>2.61</strong></td></tr></tbody></table><p>The extrapolated free-stream <code>C_d ≈ 2.84</code> is <strong>+8.9 % vs Clift&#39;s 2.61</strong>, with the residual understood as finite lattice resolution (refining <code>R = 8 → 16</code> already moved <code>C_d</code> −2.5 % toward the reference). The benchmark page details the interpretation and the reproduction commands.</p><h2 id="Where-to-go-next" tabindex="-1">Where to go next <a class="header-anchor" href="#Where-to-go-next" aria-label="Permalink to &quot;Where to go next {#Where-to-go-next}&quot;">​</a></h2><ul><li><p>The <a href="./../benchmarks/sphere-drag-3d">3D STL sphere-drag benchmark</a> — the full blockage-convergence table, the residual interpretation, and the Aqua/PBS reproduction recipe.</p></li><li><p>The <a href="./../krk-reference">KRK reference <code>Obstacle</code> block and <code>Units</code> block</a> — the STL and physical-units grammar.</p></li><li><p>The other <code>examples/geometry_stl/*.krk</code> cases — 2D cylinder cross-sections, raw-LU and physical-units twins.</p></li></ul>`,28)])])}const m=a(o,[["render",i]]);export{u as __pageData,m as default};
