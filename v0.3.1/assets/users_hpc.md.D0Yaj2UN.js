import{_ as i,o as a,c as e,an as n}from"./chunks/framework.Bwq7wkjc.js";const c=JSON.parse('{"title":"Running Kraken on HPC clusters","description":"","frontmatter":{},"headers":[],"relativePath":"users/hpc.md","filePath":"users/hpc.md","lastUpdated":null}'),t={name:"users/hpc.md"};function l(p,s,h,r,o,k){return a(),e("div",null,[...s[0]||(s[0]=[n(`<h1 id="Running-Kraken-on-HPC-clusters" tabindex="-1">Running Kraken on HPC clusters <a class="header-anchor" href="#Running-Kraken-on-HPC-clusters" aria-label="Permalink to &quot;Running Kraken on HPC clusters {#Running-Kraken-on-HPC-clusters}&quot;">​</a></h1><p>Kraken is <strong>GPU-first</strong>: production runs use <code>CUDABackend()</code> on NVIDIA GPUs, and the identical KernelAbstractions source also runs on CPU for short tests. On a typical HPC cluster the workflow is:</p><ol><li><p>install Julia in user space with <strong>juliaup</strong> (no root required),</p></li><li><p>clone or <code>Pkg.add</code> Kraken and <strong>instantiate/precompile on the login node</strong> (CPU-only — this is fine, precompilation does not need a GPU),</p></li><li><p>submit GPU runs as <strong>batch jobs</strong> through the scheduler (PBS Pro, SLURM, …).</p></li></ol><p>This page walks through each step, with a complete, working PBS Pro example (the Aqua cluster at QUT) and a short SLURM variant. For a workstation or laptop install, see <a href="./../installation">Installation</a>.</p><h2 id="User-space-installation" tabindex="-1">User-space installation <a class="header-anchor" href="#User-space-installation" aria-label="Permalink to &quot;User-space installation {#User-space-installation}&quot;">​</a></h2><h3 id="Julia-via-juliaup" tabindex="-1">Julia via juliaup <a class="header-anchor" href="#Julia-via-juliaup" aria-label="Permalink to &quot;Julia via juliaup {#Julia-via-juliaup}&quot;">​</a></h3><p>juliaup installs entirely under your home directory — no administrator needed:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -fsSL</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://install.julialang.org</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> sh</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># then make it visible in batch jobs too:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PATH</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$HOME</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/.juliaup/bin:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$PATH</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # add to ~/.bashrc</span></span></code></pre></div><h3 id="Julia-depot-location" tabindex="-1">Julia depot location <a class="header-anchor" href="#Julia-depot-location" aria-label="Permalink to &quot;Julia depot location {#Julia-depot-location}&quot;">​</a></h3><p>Julia stores packages, compiled caches, and artifacts in its <em>depot</em> (<code>~/.julia</code> by default). On clusters where home is small or slow, point it at your work/scratch filesystem — but beware of scratch <strong>purge policies</strong> (files deleted after N days of inactivity would silently wipe your packages):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Only if your home quota is too small; prefer a non-purged filesystem.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> JULIA_DEPOT_PATH</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/work/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$USER</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/.julia&quot;</span></span></code></pre></div><h3 id="Project-setup-and-precompilation" tabindex="-1">Project setup and precompilation <a class="header-anchor" href="#Project-setup-and-precompilation" aria-label="Permalink to &quot;Project setup and precompilation {#Project-setup-and-precompilation}&quot;">​</a></h3><p>Clone your project (or a Kraken checkout) into your home or work directory and instantiate it <strong>once on the login node</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;using Pkg; Pkg.instantiate(); Pkg.precompile()&#39;</span></span></code></pre></div><p>This resolves the environment, downloads packages, and compiles everything on CPU, so your first GPU job does not burn walltime on package downloads. Always run with <code>--project=.</code> (or an absolute project path) for reproducibility.</p><h3 id="Pinning-the-CUDA-runtime-driver-older-than-the-default-runtime" tabindex="-1">Pinning the CUDA runtime (driver older than the default runtime) <a class="header-anchor" href="#Pinning-the-CUDA-runtime-driver-older-than-the-default-runtime" aria-label="Permalink to &quot;Pinning the CUDA runtime (driver older than the default runtime) {#Pinning-the-CUDA-runtime-driver-older-than-the-default-runtime}&quot;">​</a></h3><p>CUDA.jl downloads a recent CUDA runtime as an artifact. If the cluster&#39;s <strong>driver is older</strong> than that runtime, jobs can fail in a confusing way: the solver starts, then <strong>kernels die during <code>CuModule</code> load with an empty error message</strong> (or a bare <code>CUDA error</code>), even though <code>CUDA.functional()</code> is <code>true</code>.</p><p>The fix is to pin the runtime to what the driver supports. Check the driver&#39;s maximum supported CUDA version with <code>nvidia-smi</code> (top-right of the header), then, once per depot:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CUDA</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CUDA</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">set_runtime_version!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">v</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;12.2&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># match your driver, e.g. driver 535 → 12.2</span></span></code></pre></div><p>Restart Julia and verify with <code>CUDA.versioninfo()</code>. Some clusters instead ship a system CUDA compatibility layer that conflicts with the artifact runtime; in that case set <code>JULIA_CUDA_USE_COMPAT=false</code> in the job script (the Aqua example below does).</p><h2 id="PBS-Pro-example-Aqua,-QUT" tabindex="-1">PBS Pro example (Aqua, QUT) <a class="header-anchor" href="#PBS-Pro-example-Aqua,-QUT" aria-label="Permalink to &quot;PBS Pro example (Aqua, QUT) {#PBS-Pro-example-Aqua,-QUT}&quot;">​</a></h2><p>Aqua uses PBS Pro with GPU nodes carrying H100 (80 GB) or A100 (40 GB) cards, selected via the <code>gpu_id</code> resource. The script below is the same mechanics as Kraken&#39;s own benchmark jobs (<code>benchmarks/krk/inc_ns/cavity_gpu_bench.pbs</code>): request one GPU, point Julia at the project, verify the GPU, run.</p><p>Save as <code>kraken_gpu.pbs</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#!/bin/bash -l</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#PBS -N kraken_cavity</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#PBS -l select=1:ncpus=4:ngpus=1:mem=32GB:gpu_id=A100</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#PBS -l walltime=04:00:00</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#PBS -j oe</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#PBS -V</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Project directory (clone/instantiate beforehand on the login node)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">WORKDIR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$HOME</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/kraken_runs/cavity</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $WORKDIR</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Job:   </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$PBS_JOBID</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Node:  $(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">hostname</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">)&quot;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Start: $(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">date</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">)&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># juliaup lives in user space; make it visible to the batch shell</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PATH</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$HOME/.juliaup/bin:$PATH</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Use the node&#39;s CUDA driver stack, not CUDA.jl&#39;s compat layer (required on Aqua)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> JULIA_CUDA_USE_COMPAT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">false</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Pre-flight GPU check: fail fast instead of wasting walltime</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nvidia-smi</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --query-gpu=name,memory.total,driver_version</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --format=csv,noheader</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  ||</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;ERROR: GPU not available&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">exit</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Ensure the environment is ready and CUDA is functional</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;using Pkg; Pkg.instantiate(); using CUDA; CUDA.versioninfo()&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># The actual run (any script that uses CUDABackend())</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=.</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> benchmarks/krk/inc_ns/cavity_gpu_bench.jl</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> 2&gt;&amp;1</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">EXIT_CODE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">$?</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;End: $(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">date</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">)  exit=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$EXIT_CODE</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">exit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $EXIT_CODE</span></span></code></pre></div><p>Line by line:</p><ul><li><p><code>#!/bin/bash -l</code> — <strong>login shell</strong>, required on PBS clusters so the module system and your profile are available.</p></li><li><p><code>select=1:ncpus=4:ngpus=1:mem=32GB:gpu_id=A100</code> — one node chunk with 4 CPU cores, one GPU, 32 GB RAM. <code>gpu_id=H100</code> requests an H100 instead; A100s usually queue faster. Request a few GB more RAM than your data size — Julia&#39;s JIT needs headroom.</p></li><li><p><code>walltime=04:00:00</code> — hard kill time. Budget the <em>first</em> run generously: the first <code>using Kraken; ...</code> on a new depot triggers precompilation of the GPU code paths (minutes, not seconds).</p></li><li><p><code>#PBS -j oe</code> — merge stdout and stderr into one log file.</p></li><li><p><code>#PBS -V</code> — export your environment variables into the job, so options can be passed at submit time with <code>qsub -v NAME=value</code> and read in the Julia script via <code>ENV</code>.</p></li><li><p><code>nvidia-smi</code> pre-flight — fail immediately if the GPU allocation is broken, rather than after minutes of Julia compilation.</p></li><li><p><code>Pkg.instantiate()</code> in the job is a cheap no-op when the login-node instantiate already ran; it protects against a stale <code>Manifest.toml</code>.</p></li></ul><p>Submit, monitor, and collect outputs:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">qsub</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> kraken_gpu.pbs</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                 # prints the job id, e.g. 1234567.pbs</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">qsub</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -v</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> CAVITY_BENCH_1024=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> kraken_gpu.pbs</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # same script, option via env var</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">qstat</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -u</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $USER                      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># queue state (Q = queued, R = running)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">qstat</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1234567</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                    # full details of one job</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># After completion: merged log appears next to the script</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ls</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> kraken_cavity.o1234567</span></span></code></pre></div><p>Simulation outputs (VTK files, checkpoints, CSV tables) land wherever your Julia script writes them — put them on the work/scratch filesystem, not in a small home quota.</p><h2 id="SLURM-variant" tabindex="-1">SLURM variant <a class="header-anchor" href="#SLURM-variant" aria-label="Permalink to &quot;SLURM variant {#SLURM-variant}&quot;">​</a></h2><p>On SLURM clusters the same job looks like this (<code>kraken_gpu.slurm</code>):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#!/bin/bash -l</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#SBATCH --job-name=kraken_cavity</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#SBATCH --nodes=1</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#SBATCH --cpus-per-task=4</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#SBATCH --gres=gpu:1</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#SBATCH --mem=32G</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#SBATCH --time=04:00:00</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#SBATCH --output=%x.o%j</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $SLURM_SUBMIT_DIR</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PATH</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$HOME/.juliaup/bin:$PATH</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">nvidia-smi</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ||</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">echo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;ERROR: GPU not available&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">exit</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; }</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;using Pkg; Pkg.instantiate()&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=.</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> my_kraken_script.jl</span></span></code></pre></div><p>Submit with <code>sbatch kraken_gpu.slurm</code>, monitor with <code>squeue -u $USER</code>, cancel with <code>scancel &lt;jobid&gt;</code>. Some sites use <code>--gpus=1</code> or a typed request such as <code>--gres=gpu:a100:1</code> — check your cluster&#39;s documentation.</p><h2 id="Containers-Apptainer" tabindex="-1">Containers (Apptainer) <a class="header-anchor" href="#Containers-Apptainer" aria-label="Permalink to &quot;Containers (Apptainer) {#Containers-Apptainer}&quot;">​</a></h2><p>On clusters where installing juliaup is impractical (restricted home, exotic OS), run Kraken from a container: convert the official <a href="https://hub.docker.com/_/julia" target="_blank" rel="noreferrer">Julia Docker image</a> to a SIF with <code>apptainer build julia.sif docker://julia:1.11</code> and run your script with <code>apptainer exec --nv julia.sif julia --project=. script.jl</code>. The <code>--nv</code> flag passes the host NVIDIA driver through to the container; keep the Julia depot bind-mounted on the host filesystem so packages persist across runs.</p><h2 id="Good-practice" tabindex="-1">Good practice <a class="header-anchor" href="#Good-practice" aria-label="Permalink to &quot;Good practice {#Good-practice}&quot;">​</a></h2><ul><li><p><strong>Never run long jobs on the login node.</strong> Login nodes are shared and processes are killed without warning; anything beyond <code>Pkg.instantiate()</code>/precompile belongs in a batch (or interactive) job.</p></li><li><p><strong>Write outputs and checkpoints to scratch/work</strong>, not home — and copy results off scratch promptly if the cluster purges inactive files.</p></li><li><p><strong>Check GPU exclusivity</strong> with <code>nvidia-smi</code> at job start: you should see your allocated GPU idle (0 % utilization, no other processes) before Kraken starts. Note that on some clusters <em>interactive</em> GPU sessions hand out MIG slices rather than full GPUs — use batch jobs for real runs.</p></li><li><p><strong>Budget walltime for first-run precompilation.</strong> A fresh depot compiles the CUDA kernels on first use; add comfortable margin to the first job, or warm the cache with a tiny run.</p></li><li><p><strong>Fail fast</strong>: keep the <code>nvidia-smi</code> and <code>CUDA.functional()</code> pre-flight checks in every job script.</p></li></ul>`,37)])])}const u=i(t,[["render",l]]);export{c as __pageData,u as default};
