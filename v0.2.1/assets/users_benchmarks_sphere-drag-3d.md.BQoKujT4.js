import{_ as a,o as s,c as t,an as i}from"./chunks/framework.DXtceF-a.js";const o="/Kraken.jl/v0.2.1/assets/sphere-drag-3d.B8-_X-QA.png",g=JSON.parse('{"title":"3D STL sphere drag (Re = 20)","description":"","frontmatter":{},"headers":[],"relativePath":"users/benchmarks/sphere-drag-3d.md","filePath":"users/benchmarks/sphere-drag-3d.md","lastUpdated":null}'),r={name:"users/benchmarks/sphere-drag-3d.md"};function l(d,e,n,c,h,p){return s(),t("div",null,[...e[0]||(e[0]=[i('<h1 id="3D-STL-sphere-drag-Re-20" tabindex="-1">3D STL sphere drag (Re = 20) <a class="header-anchor" href="#3D-STL-sphere-drag-Re-20" aria-label="Permalink to &quot;3D STL sphere drag (Re = 20) {#3D-STL-sphere-drag-Re-20}&quot;">​</a></h1><p>Validation of the generic 3D STL-obstacle flow path (<code>run_simulation</code> → <code>run_obstacle_libb_3d</code>, D3Q19 + interpolated bounce-back). A blockage sweep extrapolated to the unbounded limit gives free-stream <strong><code>C_d ≈ 2.84</code>, +8.9 % vs the Clift et al. (1978)</strong> reference <code>C_d ≈ 2.61</code> at <code>Re = 20</code> — a resolution-limited geometry gate, not a flaw in the drag path.</p><p><img src="'+o+`" alt=""></p><p>The confined <code>C_d</code> decreases monotonically as the lateral walls recede; a quadratic least-squares extrapolation of five <code>R = 16</code> blockage points (<code>D/W</code> from 20 % to 6 %, R² = 0.9998) gives the free-stream limit <code>C_d ≈ 2.84</code>. The drag coefficient is <code>C_d = 2 F_x / (u_in² A)</code>, with <code>A</code> the projected frontal area, measured by the cut-link momentum-exchange integrator on the STL wall.</p><h2 id="Why-the-residual-is-resolution-limited" tabindex="-1">Why the residual is resolution-limited <a class="header-anchor" href="#Why-the-residual-is-resolution-limited" aria-label="Permalink to &quot;Why the residual is resolution-limited {#Why-the-residual-is-resolution-limited}&quot;">​</a></h2><ul><li><p>At fixed 20 % blockage, refining <code>R = 8 → 16</code> already moved <code>C_d</code> −2.5 % (5.58 → 5.44) toward the reference; a resolution extrapolation <code>R → ∞</code> would close the gap further.</p></li><li><p>The <code>D/W = 6 %</code> case uses a shorter box (<code>Nx = 256</code> vs <code>384</code>) to fit Float64 in 80 GB, biasing its <code>C_d</code> marginally high.</p></li><li><p>A cylindrical-tube wall correction (Haberman–Sayre) over-corrects for this square duct (per-point estimates 2.96–3.24), so the true limit lies below the tube model, consistent with the ~2.8 fit.</p></li></ul><p>A combined blockage + resolution extrapolation (future work) is expected to close the residual to a few percent.</p><h2 id="Validation-path" tabindex="-1">Validation path <a class="header-anchor" href="#Validation-path" aria-label="Permalink to &quot;Validation path {#Validation-path}&quot;">​</a></h2><ol><li><p><strong>Self-consistency (local, <code>test/test_sphere_stl_drag_krk.jl</code>).</strong> The STL <code>.krk</code> sphere drag reproduces the validated analytic-sphere driver <code>run_sphere_libb_3d</code> (itself asserted against Clift) to <strong>0.4 %</strong> at matched lattice registration. The STL voxelizer is cell-centred (<code>(i-0.5)·dx</code>) while the analytic q-wall is node-centred (<code>i-1</code>); the analytic reference is placed at <code>cx = 29.5</code> so the two q-wall arrays compare the same lattice obstacle (correlate at 1.0). GPU run; CPU is a parse+voxelize smoke.</p></li><li><p><strong>Physical reference (Aqua, CUDA Float64).</strong> The blockage sweep at fixed resolution <code>R_LU = 16</code> (<code>D = 32</code>), <code>Re = 20</code>, extrapolated to <code>D/W → 0</code>.</p></li></ol><h2 id="Reproduce" tabindex="-1">Reproduce <a class="header-anchor" href="#Reproduce" aria-label="Permalink to &quot;Reproduce {#Reproduce}&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># local self-consistency twin (GPU):</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=.</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test/test_sphere_stl_drag_krk.jl</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Aqua blockage sweep (CUDA F64) — generates the .krk cases + CSV:</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">qsub</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bench/geometry_stl/sphere_drag_convergence.pbs</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # R=16: 20/14/10% (+R=8 smoke)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">qsub</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bench/geometry_stl/sphere_drag_lowblock.pbs</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">         # R=16: 8/6% (needs an 80 GB GPU)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">conda</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> kraken-v0-3-figures</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bench/geometry_stl/plot_sphere_drag.py</span></span></code></pre></div><p>Data: <code>bench/geometry_stl/sphere_drag_conv*.csv</code>. Reference: Clift, Grace &amp; Weber, <em>Bubbles, Drops, and Particles</em> (1978), drag correlation <code>C_d = (24/Re)(1 + 0.15 Re^0.687)</code>.</p>`,12)])])}const u=a(r,[["render",l]]);export{g as __pageData,u as default};
