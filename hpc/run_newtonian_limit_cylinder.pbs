#!/bin/bash -l
#PBS -N krk_newt_limit
#PBS -l select=1:ncpus=8:ngpus=1:mem=48GB:gpu_id=H100
#PBS -l walltime=04:00:00
#PBS -j oe
#PBS -m ae

cd $PBS_O_WORKDIR

export PATH="$HOME/.juliaup/bin:$PATH"
export JULIA_CUDA_USE_COMPAT=false
export JULIA_PKG_PRECOMPILE_AUTO=0
export JULIA_DEPOT_PATH="${PBS_O_WORKDIR}/../julia_depot:$HOME/.julia"
mkdir -p "${PBS_O_WORKDIR}/../julia_depot"

export KRAKEN_BACKEND="${KRAKEN_BACKEND:-cuda}"
export KRAKEN_R="${KRAKEN_R:-20}"
export KRAKEN_U_MEAN="${KRAKEN_U_MEAN:-0.005}"
export KRAKEN_BETA="${KRAKEN_BETA:-0.59}"
export KRAKEN_WI="${KRAKEN_WI:-0.001}"
export KRAKEN_STEPS_PER_R="${KRAKEN_STEPS_PER_R:-2000}"
export KRAKEN_AVG_DIVISOR="${KRAKEN_AVG_DIVISOR:-10}"
export KRAKEN_DRAG_STRIDE="${KRAKEN_DRAG_STRIDE:-100}"
export KRAKEN_SOLVENT_MAGIC="${KRAKEN_SOLVENT_MAGIC:-0.1875}"
export KRAKEN_CONFORMATION_MAGIC="${KRAKEN_CONFORMATION_MAGIC:-1e-6}"
export KRAKEN_RESULTS_DIR="${KRAKEN_RESULTS_DIR:-results/newtonian_limit_cylinder_${PBS_JOBID:-local}}"

nvidia-smi || { echo "ERROR: GPU not available"; exit 1; }
if [ "${KRAKEN_SKIP_PKG:-0}" != "1" ]; then
    julia --project=. -e 'using Pkg; Pkg.resolve(); Pkg.instantiate(); Pkg.precompile()'
fi

mkdir -p results
echo "=== Newtonian-limit cylinder force modes at $(date) ==="
julia --project=. hpc/newtonian_limit_cylinder.jl 2>&1 \
    | tee "results/newtonian_limit_cylinder_${PBS_JOBID:-local}.txt"
echo "=== Done at $(date) ==="
