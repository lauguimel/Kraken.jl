import{_ as t,o as a,c as o,an as s}from"./chunks/framework.DBFeC3Rc.js";const u=JSON.parse('{"title":"KRK Reference","description":"","frontmatter":{},"headers":[],"relativePath":"users/krk-reference.md","filePath":"users/krk-reference.md","lastUpdated":null}'),n={name:"users/krk-reference.md"};function i(d,e,c,l,r,p){return a(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="KRK-Reference" tabindex="-1">KRK Reference <a class="header-anchor" href="#KRK-Reference" aria-label="Permalink to &quot;KRK Reference {#KRK-Reference}&quot;">​</a></h1><p>The <code>.krk</code> file is Kraken&#39;s plain-text case format. One file fully describes a simulation — geometry, physics, boundary conditions, run length and output — in a declarative, block-per-line syntax inspired by Gerris. You never write Julia to run a standard case: you write a <code>.krk</code> file and hand it to <code>run_simulation</code>.</p><h2 id="Run-your-first-.krk-in-5-minutes" tabindex="-1">Run your first <code>.krk</code> in 5 minutes <a class="header-anchor" href="#Run-your-first-.krk-in-5-minutes" aria-label="Permalink to &quot;Run your first `.krk` in 5 minutes {#Run-your-first-.krk-in-5-minutes}&quot;">​</a></h2><p>Create a file <code>mycase.krk</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># Lid-driven cavity at Re = 100</span></span>\n<span class="line"><span>Simulation cavity D2Q9</span></span>\n<span class="line"><span>Domain  L = 1.0 x 1.0  N = 128 x 128</span></span>\n<span class="line"><span>Physics nu = 0.128</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>Boundary north velocity(ux = 0.1, uy = 0)</span></span>\n<span class="line"><span>Boundary south wall</span></span>\n<span class="line"><span>Boundary east  wall</span></span>\n<span class="line"><span>Boundary west  wall</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>Run 60000 steps</span></span>\n<span class="line"><span>Output vtk every 10000 [rho, ux, uy]</span></span></code></pre></div><p>Run it from Julia:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Kraken</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">run_simulation</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mycase.krk&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>That is the whole loop: the runner parses the file, builds the lattice, picks the right backend driver from the lattice and the active modules, iterates, and writes VTK snapshots into <code>output/</code>. Open the <code>.vtr</code> files in ParaView to view the field.</p><p>A few conventions hold throughout:</p><ul><li><p><strong>Lines are blocks.</strong> Each non-blank, non-comment line is one block. Comments start with <code>#</code>. Brace blocks (<code>{ ... }</code>) may span multiple lines.</p></li><li><p><strong>One <code>Simulation</code> line, one <code>Domain</code>, one <code>Physics</code>.</strong> Most other blocks (<code>Boundary</code>, <code>Obstacle</code>, <code>Output</code>, <code>Refine</code>) may appear several times.</p></li><li><p><strong>Lattice units.</strong> Unless a <code>Units { ... }</code> block is present, every number is in lattice units (LU). The diffusive/acoustic scaling that maps LU to physical units is your responsibility — or you delegate it to a <code>Units</code> block (see below).</p></li><li><p><strong>Expressions are allowed</strong> anywhere a number is. <code>4*U*y*(H - y)/H^2</code>, <code>0.05*sin(2*pi*x)</code>, and references to <code>Define</code>d variables all evaluate at parse time (boundary-profile expressions also see the spatial coordinates <code>x</code>, <code>y</code>, <code>z</code>).</p></li></ul><h2 id="Block-catalogue" tabindex="-1">Block catalogue <a class="header-anchor" href="#Block-catalogue" aria-label="Permalink to &quot;Block catalogue {#Block-catalogue}&quot;">​</a></h2><p>The parser recognises the following block keywords (see <code>src/io/kraken_parser.jl</code>). Each is documented below with its keywords and a minimal worked example. Two directives — <code>Preset</code> and <code>Sweep</code> — are pre-expanded before the main parse and are documented under <a href="./@ref">Presets</a> and <a href="/Kraken.jl/stable/users/krk-reference#Sweeps">Sweeps</a>.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Block</th><th style="text-align:right;">Multiplicity</th><th style="text-align:right;">Purpose</th></tr></thead><tbody><tr><td style="text-align:right;"><code>Simulation</code></td><td style="text-align:right;">exactly 1</td><td style="text-align:right;">case name + lattice (<code>D2Q9</code> / <code>D3Q19</code>)</td></tr><tr><td style="text-align:right;"><code>Domain</code></td><td style="text-align:right;">exactly 1</td><td style="text-align:right;">physical size <code>L</code> and grid resolution <code>N</code></td></tr><tr><td style="text-align:right;"><code>Physics</code></td><td style="text-align:right;">1</td><td style="text-align:right;">transport coefficients (<code>nu</code>, <code>alpha</code>, <code>Pr</code>, …)</td></tr><tr><td style="text-align:right;"><code>Define</code></td><td style="text-align:right;">many</td><td style="text-align:right;">named scalar constants for reuse in expressions</td></tr><tr><td style="text-align:right;"><code>Units</code></td><td style="text-align:right;">≤ 1</td><td style="text-align:right;">physical-units → LU bridge (auto <code>nu</code>, <code>u_LU</code>)</td></tr><tr><td style="text-align:right;"><code>Module</code></td><td style="text-align:right;">many</td><td style="text-align:right;">activate a solver module (<code>thermal</code>, <code>axisymmetric</code>, …)</td></tr><tr><td style="text-align:right;"><code>Obstacle</code></td><td style="text-align:right;">many</td><td style="text-align:right;">solid region (implicit inequality or STL)</td></tr><tr><td style="text-align:right;"><code>Fluid</code></td><td style="text-align:right;">many</td><td style="text-align:right;">fluid-tagged region (two-phase / init by region)</td></tr><tr><td style="text-align:right;"><code>Boundary</code></td><td style="text-align:right;">many</td><td style="text-align:right;">a domain-face boundary condition</td></tr><tr><td style="text-align:right;"><code>Initial</code></td><td style="text-align:right;">≤ 1</td><td style="text-align:right;">initial field expressions (<code>ux</code>, <code>uy</code>, <code>C</code>, …)</td></tr><tr><td style="text-align:right;"><code>Velocity</code></td><td style="text-align:right;">≤ 1</td><td style="text-align:right;">frozen velocity field (advection-only module)</td></tr><tr><td style="text-align:right;"><code>Refine</code></td><td style="text-align:right;">many</td><td style="text-align:right;">a patch-based 2:1 grid-refinement region</td></tr><tr><td style="text-align:right;"><code>Rheology</code></td><td style="text-align:right;">many</td><td style="text-align:right;">non-Newtonian / viscoelastic constitutive model</td></tr><tr><td style="text-align:right;"><code>Setup</code></td><td style="text-align:right;">1</td><td style="text-align:right;">extra driver helper key/values</td></tr><tr><td style="text-align:right;"><code>Run</code></td><td style="text-align:right;">exactly 1</td><td style="text-align:right;">number of time steps</td></tr><tr><td style="text-align:right;"><code>Output</code></td><td style="text-align:right;">many</td><td style="text-align:right;">field snapshots (<code>vtk</code> / <code>png</code> / <code>gif</code>)</td></tr><tr><td style="text-align:right;"><code>Diagnostics</code></td><td style="text-align:right;">≤ 1</td><td style="text-align:right;">scalar time-series columns (<code>drag</code>, <code>lift</code>, …)</td></tr></tbody></table><h3 id="Simulation" tabindex="-1"><code>Simulation</code> <a class="header-anchor" href="#Simulation" aria-label="Permalink to &quot;`Simulation` {#Simulation}&quot;">​</a></h3><p><code>Simulation &lt;name&gt; &lt;lattice&gt;</code> — the case name (used for output filenames) and the lattice stencil. Valid lattices: <strong><code>D2Q9</code></strong> (2D) and <strong><code>D3Q19</code></strong> (3D). Exactly one per file.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Simulation cavity D2Q9</span></span></code></pre></div><h3 id="Domain" tabindex="-1"><code>Domain</code> <a class="header-anchor" href="#Domain" aria-label="Permalink to &quot;`Domain` {#Domain}&quot;">​</a></h3><p><code>Domain L = &lt;Lx&gt; x &lt;Ly&gt; [x &lt;Lz&gt;] N = &lt;Nx&gt; x &lt;Ny&gt; [x &lt;Nz&gt;]</code> — physical extent and grid resolution. The third (<code>Lz</code>/<code>Nz</code>) factor is read only for <code>D3Q19</code>. Values may be numbers or <code>Define</code>d variables.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Domain L = 10.0 x 2.5  N = 200 x 50          # 2D</span></span>\n<span class="line"><span>Domain L = 1.0 x 1.0 x 1.0  N = 64 x 64 x 64 # 3D</span></span></code></pre></div><p>When a <code>Units</code> block is present, <code>L</code> must equal <code>N</code> in the active dimensions so that the geometry coordinates are themselves lattice units.</p><h3 id="Physics" tabindex="-1"><code>Physics</code> <a class="header-anchor" href="#Physics" aria-label="Permalink to &quot;`Physics` {#Physics}&quot;">​</a></h3><p><code>Physics key = value ...</code> — transport coefficients, all in lattice units. Any <code>key = value</code> pair is accepted; the keys consumed by the drivers include:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Key</th><th style="text-align:right;">Meaning</th></tr></thead><tbody><tr><td style="text-align:right;"><code>nu</code></td><td style="text-align:right;">kinematic viscosity (LU). <code>nu = auto</code> defers to a <code>Units</code> block</td></tr><tr><td style="text-align:right;"><code>alpha</code></td><td style="text-align:right;">thermal diffusivity (thermal module)</td></tr><tr><td style="text-align:right;"><code>Pr</code></td><td style="text-align:right;">Prandtl number (thermal module)</td></tr><tr><td style="text-align:right;"><code>Ra</code></td><td style="text-align:right;">Rayleigh number (thermal module)</td></tr><tr><td style="text-align:right;"><code>Fx</code>, <code>Fy</code></td><td style="text-align:right;">constant body force per direction (e.g. Poiseuille drive)</td></tr></tbody></table><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Physics nu = 0.1 alpha = 0.01           # forced-conduction</span></span>\n<span class="line"><span>Physics nu = 0.02 Pr = 0.71 Ra = 1e5    # natural convection</span></span>\n<span class="line"><span>Physics nu = 0.1  Fx = 1e-5             # body-force-driven Poiseuille</span></span></code></pre></div><h3 id="Define" tabindex="-1"><code>Define</code> <a class="header-anchor" href="#Define" aria-label="Permalink to &quot;`Define` {#Define}&quot;">​</a></h3><p><code>Define &lt;NAME&gt; = &lt;number&gt;</code> — a named constant. Defines are visible to every expression parsed afterwards (boundary profiles, obstacle conditions, <code>Domain</code> sizes). They are also the override points for parametric runs: a keyword passed to <code>run_simulation</code>/<code>parse_kraken</code> overrides the matching <code>Define</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Define U = 0.05</span></span>\n<span class="line"><span>Define R = 0.5</span></span>\n<span class="line"><span>Boundary west velocity(ux = 4*U*y*(H - y)/H^2, uy = 0)</span></span></code></pre></div><h3 id="Units" tabindex="-1"><code>Units</code> <a class="header-anchor" href="#Units" aria-label="Permalink to &quot;`Units` {#Units}&quot;">​</a></h3><p><code>Units { length = &lt;unit&gt; L_ref = &lt;val&gt; R_LU = &lt;int&gt; Re = &lt;val&gt; [scaling = &lt;acoustic|diffusive|auto&gt;] [L_up = ...] [L_down = ...] }</code> — the physical-units bridge. Given a reference length <code>L_ref</code> in physical units (resolved to <code>R_LU</code> lattice cells) and a target Reynolds number, the bridge solves for the LU viscosity and inflow speed and injects them as the variable <code>u_LU</code> and as <code>Physics nu</code>. Required keys: <code>length</code>, <code>L_ref</code>, <code>R_LU</code>, <code>Re</code>. The block currently requires at least one STL geometry region and <code>Domain L == N</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Units { length = mm  L_ref = 0.2  R_LU = 4  Re = 1.0  scaling = acoustic }</span></span>\n<span class="line"><span>Physics nu = auto</span></span>\n<span class="line"><span>Boundary west velocity(ux = u_LU, uy = 0)</span></span></code></pre></div><h3 id="Module" tabindex="-1"><code>Module</code> <a class="header-anchor" href="#Module" aria-label="Permalink to &quot;`Module` {#Module}&quot;">​</a></h3><p><code>Module &lt;name&gt;</code> — activate a solver module; may appear several times. The runner dispatches on the active set. Documented module names include <strong><code>thermal</code></strong> (double-distribution temperature + Boussinesq buoyancy), <strong><code>axisymmetric</code></strong> (<code>(z, r)</code> mesh with <code>z</code>/<code>wall</code>/<code>axis</code> face aliases), <strong><code>advection_only</code></strong> (frozen- velocity VOF advection, needs a <code>Velocity</code> and <code>Initial { C = ... }</code>), and <strong><code>twophase_vof</code></strong> (two-phase with surface tension).</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Module thermal</span></span></code></pre></div><h3 id="Obstacle-/-Fluid" tabindex="-1"><code>Obstacle</code> / <code>Fluid</code> <a class="header-anchor" href="#Obstacle-/-Fluid" aria-label="Permalink to &quot;`Obstacle` / `Fluid` {#Obstacle-/-Fluid}&quot;">​</a></h3><p><code>Obstacle &lt;name&gt; [wall=&lt;wall|libb&gt;] { &lt;inequality&gt; }</code> or <code>Obstacle &lt;name&gt; [wall=libb] stl(file = &quot;...&quot;, [scale=...], [translate=[x,y,z]], [z_slice=...])</code> — a solid region, defined either by an implicit inequality (true = solid) or by an STL mesh. The <code>wall=</code> selector chooses the wall treatment: <code>wall</code> (default, half-way bounce-back) or <code>libb</code> (interpolated/linear bounce-back, needed for curved STL surfaces and cut-link drag). <code>Fluid</code> uses the same syntax to tag a fluid sub-region.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Obstacle cylinder { (x - cx)^2 + (y - cy)^2 &lt;= R^2 }</span></span>\n<span class="line"><span>Obstacle sph wall=libb stl(file = &quot;examples/geometry_stl/sphere.stl&quot;, scale = 20.0)</span></span></code></pre></div><h3 id="Boundary" tabindex="-1"><code>Boundary</code> <a class="header-anchor" href="#Boundary" aria-label="Permalink to &quot;`Boundary` {#Boundary}&quot;">​</a></h3><p><code>Boundary &lt;face&gt; &lt;type&gt;(&lt;params&gt;)</code> or <code>Boundary &lt;face&gt; &lt;type&gt; [extra = ...]</code>, plus the axis shorthand <code>Boundary &lt;x|y|z&gt; periodic</code>. Faces are <strong><code>west</code> / <code>east</code> / <code>south</code> / <code>north</code></strong> (2D) plus <strong><code>top</code> / <code>bottom</code></strong> (3D); <code>front</code> / <code>back</code> are legacy 3D aliases. Boundary types: <strong><code>wall</code></strong> (no-slip bounce-back), <strong><code>velocity</code></strong> (Zou–He imposed velocity), <strong><code>pressure</code></strong> (imposed density/pressure outlet), <strong><code>periodic</code></strong>, <strong><code>outflow</code></strong>, <strong><code>neumann</code></strong>, <strong><code>symmetry</code></strong>. A trailing <code>T = ...</code> sets a thermal Dirichlet/condition on the face. Velocity-profile parameters may be spatial expressions in <code>x</code>, <code>y</code>, <code>z</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Boundary north velocity(ux = 0.1, uy = 0)</span></span>\n<span class="line"><span>Boundary east  pressure(rho = 1.0)</span></span>\n<span class="line"><span>Boundary west  wall T = 1.0          # no-slip + hot Dirichlet</span></span>\n<span class="line"><span>Boundary x periodic                  # west+east periodic in one line</span></span></code></pre></div><h3 id="Initial-/-Velocity" tabindex="-1"><code>Initial</code> / <code>Velocity</code> <a class="header-anchor" href="#Initial-/-Velocity" aria-label="Permalink to &quot;`Initial` / `Velocity` {#Initial-/-Velocity}&quot;">​</a></h3><p><code>Initial { &lt;field&gt; = &lt;expr&gt; ... }</code> seeds the initial fields; <code>Velocity { ux = ... uy = ... }</code> supplies a frozen velocity for the advection-only module. Both use the same brace syntax, and expressions see <code>x</code>, <code>y</code>, <code>z</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Initial { ux = 0.05*sin(2*pi*x)*cos(2*pi*y) uy = -0.05*cos(2*pi*x)*sin(2*pi*y) }</span></span></code></pre></div><h3 id="Refine" tabindex="-1"><code>Refine</code> <a class="header-anchor" href="#Refine" aria-label="Permalink to &quot;`Refine` {#Refine}&quot;">​</a></h3><p><code>Refine &lt;name&gt; { region = [x0, y0, x1, y1] (or 6 coords in 3D), ratio = 2, parent = base, [criterion options] }</code> — a patch-based, 2:1 nested refinement region with Filippova–Hänel rescaling at the coarse–fine interface for second-order accuracy. Only a <code>balance = 1</code> (2:1) ratio is supported. Optional adaptive-criterion keys (<code>update_every</code>, <code>pad</code>, <code>max_growth</code>, <code>shrink_margin</code>) drive dynamic patches. Works for 2D/3D, isothermal and thermal.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Refine corner_patch {</span></span>\n<span class="line"><span>    region = [0.5, 0.5, 1.0, 1.0],</span></span>\n<span class="line"><span>    ratio  = 2,</span></span>\n<span class="line"><span>    parent = base</span></span>\n<span class="line"><span>}</span></span></code></pre></div><h3 id="Rheology" tabindex="-1"><code>Rheology</code> <a class="header-anchor" href="#Rheology" aria-label="Permalink to &quot;`Rheology` {#Rheology}&quot;">​</a></h3><p><code>Rheology [phase] &lt;model&gt; { key = value ... }</code> — a non-Newtonian or viscoelastic constitutive law. Optional <code>phase</code> is <code>liquid</code> / <code>gas</code> / <code>default</code>. Known models: <code>newtonian</code>, <code>power_law</code>, <code>carreau</code>, <code>cross</code>, <code>bingham</code>, <code>herschel_bulkley</code>, <code>oldroyd_b</code>, <code>fene_p</code>, <code>saramito</code>. Each model reads its own parameters (e.g. <code>power_law</code> reads <code>K</code>, <code>n</code>; <code>oldroyd_b</code> reads <code>nu_s</code>, <code>nu_p</code>, <code>lambda</code>). A thermal coupling is inferred from <code>E_a</code> (Arrhenius) or <code>C1</code>/<code>C2</code> (WLF).</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Rheology power_law { K = 0.1  n = 0.5 }</span></span>\n<span class="line"><span>Rheology oldroyd_b { nu_s = 0.0885  nu_p = 0.0615  lambda = 6000 }</span></span></code></pre></div><h3 id="Run" tabindex="-1"><code>Run</code> <a class="header-anchor" href="#Run" aria-label="Permalink to &quot;`Run` {#Run}&quot;">​</a></h3><p><code>Run &lt;N&gt; steps</code> — the number of LBM time steps. Exactly one per file.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Run 60000 steps</span></span></code></pre></div><h3 id="Output" tabindex="-1"><code>Output</code> <a class="header-anchor" href="#Output" aria-label="Permalink to &quot;`Output` {#Output}&quot;">​</a></h3><p><code>Output &lt;format&gt; every &lt;N&gt; [field1, field2, ...] [fps = &lt;N&gt;]</code> — snapshot writer. Formats: <strong><code>vtk</code></strong> (<code>.vtr</code>, open in ParaView), <strong><code>png</code></strong>, <strong><code>gif</code></strong> (the <code>fps</code> parameter applies to <code>gif</code>). Fields are the lattice fields to dump (<code>rho</code>, <code>ux</code>, <code>uy</code>, <code>uz</code>, <code>T</code>, …). Several <code>Output</code> lines may coexist. Output lands in <code>output/</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Output vtk every 10000 [rho, ux, uy]</span></span>\n<span class="line"><span>Output vtk every 5000 [rho, ux, uy, uz, T]</span></span></code></pre></div><h3 id="Diagnostics" tabindex="-1"><code>Diagnostics</code> <a class="header-anchor" href="#Diagnostics" aria-label="Permalink to &quot;`Diagnostics` {#Diagnostics}&quot;">​</a></h3><p><code>Diagnostics every &lt;N&gt; [col1, col2, ...]</code> — a scalar time-series log. Columns seen in the examples include <code>step</code>, <code>drag</code>, <code>lift</code>. Useful for tracking the drag/lift on an obstacle as the run converges.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Diagnostics every 100 [step, drag, lift]</span></span></code></pre></div><h3 id="Setup" tabindex="-1"><code>Setup</code> <a class="header-anchor" href="#Setup" aria-label="Permalink to &quot;`Setup` {#Setup}&quot;">​</a></h3><p><code>Setup { key = value ... }</code> — extra driver helper key/values merged into the driver call. This is an escape hatch for driver-specific options not covered by a dedicated block; document the keys against the target driver before relying on them.</p><h2 id="Presets" tabindex="-1">Presets <a class="header-anchor" href="#Presets" aria-label="Permalink to &quot;Presets {#Presets}&quot;">​</a></h2><p>A <code>Preset &lt;name&gt;</code> line expands, before parsing, into a full set of <code>.krk</code> lines for a canonical case. You can then override any expanded block by re-stating it after the <code>Preset</code> line — the later block wins. The presets shipped in <code>kraken_parser.jl</code> (<code>_expand_preset</code>) are:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Preset</th><th style="text-align:right;">Case</th></tr></thead><tbody><tr><td style="text-align:right;"><code>cavity_2d</code></td><td style="text-align:right;">lid-driven cavity, <code>128²</code>, lid <code>ux = 0.1</code></td></tr><tr><td style="text-align:right;"><code>poiseuille_2d</code></td><td style="text-align:right;">body-force Poiseuille channel, periodic in <code>x</code></td></tr><tr><td style="text-align:right;"><code>couette_2d</code></td><td style="text-align:right;">Couette shear, moving top wall</td></tr><tr><td style="text-align:right;"><code>taylor_green_2d</code></td><td style="text-align:right;">doubly-periodic Taylor–Green vortex</td></tr><tr><td style="text-align:right;"><code>rayleigh_benard_2d</code></td><td style="text-align:right;">hot-bottom / cold-top Rayleigh–Bénard, <code>Module thermal</code></td></tr></tbody></table><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Preset rayleigh_benard_2d</span></span>\n<span class="line"><span>Run 30000 steps</span></span>\n<span class="line"><span>Output vtk every 5000 [rho, ux, uy, T]</span></span></code></pre></div><h2 id="Sweeps" tabindex="-1">Sweeps <a class="header-anchor" href="#Sweeps" aria-label="Permalink to &quot;Sweeps {#Sweeps}&quot;">​</a></h2><p>A <code>Sweep param = [a, b, c]</code> directive turns one file into a family of cases. Use <code>parse_kraken_sweep</code> (or <code>run_simulation</code> over the sweep) to materialise one <code>SimulationSetup</code> per value; the swept name overrides the matching <code>Define</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Define Re = 100</span></span>\n<span class="line"><span>Sweep Re = [100, 400, 1000]</span></span></code></pre></div><h2 id="Things-to-know-/-current-gaps" tabindex="-1">Things to know / current gaps <a class="header-anchor" href="#Things-to-know-/-current-gaps" aria-label="Permalink to &quot;Things to know / current gaps {#Things-to-know-/-current-gaps}&quot;">​</a></h2><ul><li><p><strong><code>natural_convection</code> is not a preset.</strong> The thermal-cavity benchmark is built from explicit blocks (<code>Module thermal</code> + hot/cold <code>Boundary ... T = ...</code>), not a preset — <code>_expand_preset</code> knows only the five names above. The <a href="./benchmarks/thermal-natural-convection">thermal natural-convection benchmark</a> writes the case out explicitly. Adding <code>natural_convection_2d</code>/<code>_3d</code> presets is a natural follow-up.</p></li><li><p><strong><code>Setup</code> is intentionally open-ended.</strong> Its keys are driver-specific and are not enumerated by the parser; treat it as an advanced escape hatch and validate the keys against the driver you target.</p></li><li><p><strong><code>Units</code> requires an STL region</strong> and <code>Domain L == N</code> today; the implicit- geometry path runs in pure lattice units.</p></li></ul>',68)])])}const g=t(n,[["render",i]]);export{u as __pageData,g as default};
